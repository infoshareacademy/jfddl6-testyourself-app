import React from 'react'

import { auth, googleProvider } from './../firebase'

class Auth extends React.Component {
    render() {
        return (
            <div>

            </div>
        )
    }
}

export default Auth